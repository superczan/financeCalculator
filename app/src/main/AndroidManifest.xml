<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shijanshrest.financecalculator" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
       >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"  android:configChanges="keyboardHidden|orientation|screenSize" android:logo="@drawable/logo12">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".annuityCalculation" android:label="FIN 101" android:logo="@drawable/logo12">

        </activity>

        <activity android:name=".AnnuityDue" android:label="FIN 101"
            android:logo="@drawable/logo12">

            </activity>

        <activity android:name=".Perpetuity" android:label="FIN 101" android:logo="@drawable/logo12"></activity>

    </application>

</manifest>
